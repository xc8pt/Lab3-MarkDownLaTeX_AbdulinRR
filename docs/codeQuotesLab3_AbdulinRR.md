Блоки кода
Встроенный (inline) код:
Команда `git status` показывает состояние репозитория.
Команда `git remote` просмотр репоззитория git.
Многострочный код:
```python
print("Hello, Markdown!")
```
```csharp
Console.Write("Введите своё имя: ");
string? name = Console.ReadLine();
Console.WriteLine($"Привет, {name}");
```
```
Console.Write("Введите своё имя: ");
string? name = Console.ReadLine();
Console.WriteLine($"Привет, {name}");
```
Можно указать язык: python, js, bash, csharp и др.
Цитаты
Цитата создаётся символом >:
> Это цитата в Markdown, буква Ю.
Вложенная цитата:
> Первый уровень бла бла бла 
>> Второй уровень бла бла бла 